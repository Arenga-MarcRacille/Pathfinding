@model IEnumerable<Pathfinding.Models.MapData>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nodes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Edges)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MapsId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nodes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Edges)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MapsId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="MapDetails" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
